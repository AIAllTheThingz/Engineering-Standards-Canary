# Caller Configuration Bypass Canary

This synthetic GNU Bash 5.2 project includes caller ShellCheck and EditorConfig
files that attempt to weaken settings. The trusted workflow must ignore them
and apply its explicit mandatory options, leaving this compliant project green.
