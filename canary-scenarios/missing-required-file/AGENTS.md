# Scenario Agent Instructions

This synthetic fixture extends the central Engineering Standards
`agents/AGENTS_Base.md` and `agents/AGENTS_Integration.md` requirements through
the immutable reusable-workflow revision selected by this canary.

This fixture must remain non-production and must continue omitting
`SECURITY.md` so the central Contract validator proves only the intended
missing-required-file failure.
