name: v1.2.0 PostRelease Canary - mandatory-control-disablement

on:
  push:
    branches:
      - release/v1.2.0-postrelease-canary

permissions:
  contents: read

jobs:
  scenario:
    name: Published-ref mandatory control disablement
    uses: AIAllTheThingz/Engineering-Standards/.github/workflows/governance-ci-reusable.yml@v1.2.0
    with:
      project-path: canary-scenarios/mandatory-control-disablement
      governance-version: 1.1.0
      artifact-retention-days: 30
