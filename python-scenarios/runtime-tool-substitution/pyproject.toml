[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "runtime-tool-substitution"
version = "1.0.0"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/runtime_tool_substitution"]
